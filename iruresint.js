// Example function definition
function ends(index) {
    // Some logic to compute or retrieve a value based on 'index'
    return index + 10; // Example logic, could be anything
}

// Usage
var i = 5;
var e = ends(i); // e will now hold the result of ends(5), which is 15 in this example
console.log(e); // Output: 15
